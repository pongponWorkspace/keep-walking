# เกณฑ์ให้คะแนนย่านเปิดตัว (Launch Criteria)

Task: P1-F01-T02 · เจ้าของ: level-designer · สถานะ: ผ่านการใช้งานจริงใน P1-F01-T06 (นับรายเขต + heatmap) และ P1-F01-T07 (coverage report) แล้ว — แก้ไขรอบ T07 (2026-09-23) ตาม P1-H08 cross-read (ระยะเดิน, หัวข้อ 2/4.2) และแก้บั๊กสูตร `transitAccessScore` (หัวข้อ 4.3) ก่อนใช้จัดอันดับจริง
อ้างอิง: `product/prd/F01-coverage-survey.md` (หัวข้อ 3–5 และ 8.1, cross-read PM-S07) · `tools/coverage/METHOD.md` (โครงสร้าง candidates, `rep_point`) · `tools/coverage/README.md` หัวข้อ 10.3–10.4 (วิธีคำนวณระยะเดินจริงที่ใช้รัน) · `design/levels/dungeon-rules.md` (นิยาม dungeon ที่ใช้ได้) · `config/balance/unlocks.json#home.farDungeonThreshold_m` · GDD "กลยุทธ์การเปิด", "10 นาทีแรกของคนใหม่"

หลักการ: เอกสารนี้ **ไม่สร้างเกณฑ์ Go/No-go ใหม่** (นั่นคือ PRD F01 หัวข้อ 4 ที่ product-manager เป็นเจ้าของ) แต่สร้าง **เครื่องมือจัดอันดับ** ว่า 79 เขต/อำเภอไหนน่าเป็น "ย่านเปิดตัว 2–3 ย่าน" ที่ P1-F01-T07 จะเอาไปตรวจกับเกณฑ์ PRD อีกที หน่วยวัดทุกตัวในเอกสารนี้ใช้นิยามเดียวกับ PRD F01 หัวข้อ 3 ทุกตัว (ห้ามมีหน่วยที่สอง)

## 1. นิยาม "ย่าน" (ต้องตรงกับ location-engineer และ PRD)

**ย่าน = เขต (กรุงเทพฯ) หรือ อำเภอ (5 จังหวัดปริมณฑล)** ตาม `tools/coverage/METHOD.md` หัวข้อ 2 (relation `admin_level=6`) และ PRD F01 หัวข้อ 3 — ไม่ใช่ grid ที่ตั้งเอง เพื่อให้อ้างอิงข้อมูลประชากรและเขตปกครองที่ทางการเผยแพร่ได้ตรงกัน

- ใช้ `district_osm_id` เป็นตัวระบุย่าน ไม่ใช้ชื่อ (ชื่อซ้ำข้ามจังหวัดได้ตาม METHOD 9.3)
- candidate ที่มี `multi_district = true` (คร่อมสองเขต) นับเข้า**ทั้งสองย่าน**สำหรับตัวชี้วัดจำนวน/ความหนาแน่น แต่ประชากรที่อยู่ในโซนเขียวของ candidate นั้นคำนวณแยกตามสัดส่วนพื้นที่ที่ทับแต่ละเขต (location-engineer เป็นผู้คำนวณจริงใน T06 เอกสารนี้กำหนดหลักการเท่านั้น)
- รวม 79 ย่านตาม METHOD หัวข้อ 2 (กรุงเทพฯ 50 เขต + ปริมณฑล 29 อำเภอ)

## 2. หน่วยวัดร่วม (อ้างจาก PRD F01 หัวข้อ 3 ทุกตัว — ไม่เขียนนิยามซ้ำสองที่)

| หน่วยวัด | นิยาม (อ้าง PRD F01 หัวข้อ 3 และ 8.1 — แก้ไขรอบ T07 ตาม P1-H08) |
| --- | --- |
| ระยะจาก dungeon ใกล้สุด | **วิธีหลัก (ใช้จริงใน T06):** ระยะเดินบนโครงข่ายทางเดิน/ถนนจริงจาก `rep_point` ของ dungeon ที่ใช้ได้ ไปยังจุดตัวแทนประชากร คำนวณด้วย multi-source Dijkstra บนกราฟจาก D1 (`thailand-260901.osm.pbf`) ตาม PRD 8.1 — **ไม่ใช่ค่าประมาณจากเส้นตรง** (`tools/coverage/README.md` หัวข้อ 10.3: 3.16 ล้าน node, snap ค่ากลาง 41.8 ม.) · **ทางสำรอง (ไม่ได้ใช้ในรอบนี้):** ระยะเส้นตรง × **1.4** (ค่าเดียวที่ล็อกไว้ ไม่ใช่ช่วง 1.3–1.4 ที่เอกสารรุ่นก่อนอ้างผิด — PRD หัวข้อ 8.1 ล็อกโดย P1-H08) ใช้เฉพาะถ้า network routing ทำไม่ทัน ซึ่งไม่เกิดขึ้นจริงในรอบนี้ · ค่าเทียบเพื่อสอบทานเท่านั้น (ไม่ใช้ตัดสิน): route factor ที่วัดได้จริง (ระยะ network ÷ เส้นตรง) ค่ากลาง **1.446** ใกล้เคียงค่า 1.4 ที่ล็อกไว้ |
| dungeon ที่ใช้ได้ (valid) | ผ่านทุกกฎใน `dungeon-rules.md` และ `tools/coverage/METHOD.md`: tag 6 ชุด, `area.minArea_m2 ≤ area_m2 ≤ area.maxArea_m2`, ไม่ติด blocklist, ไม่ติด `review_required` ที่ยังไม่ผ่าน HUMAN |
| โซนระยะทาง (เขียว/เหลือง/แดง) | ตามตาราง PRD F01 หัวข้อ 3 (เขียว ≤ 800 ม., เหลือง 800–3,000 ม., แดง > 3,000 ม. หรือไม่มี dungeon ในย่าน) |
| ประชากรในย่าน | ตัวเลขจาก WorldPop (METHOD S2) ใช้เชิงเปรียบเทียบระหว่างย่าน ไม่ใช่ตัวเลขสัมบูรณ์ |
| preset | ตาม `dungeon-rules.md` หัวข้อ 12 (สวนใหญ่ / ตลาด / สวนหย่อม เป็นหลัก, ริมน้ำ/ริมคลองเป็นชั้นที่สอง) |

## 3. สูตรให้คะแนนย่าน (Launch Score)

ใช้จัดอันดับ 79 ย่านเพื่อเลือกย่านที่น่าพิจารณาก่อนส่งเข้าตรวจเกณฑ์ Go/No-go ของ PRD (หัวข้อ 4.1–4.3) **ไม่ใช่ตัวตัดสิน Go/No-go เอง** — คะแนนนี้เป็นเครื่องมือช่วยเรียงลำดับความสำคัญตอนเขียน coverage report (P1-F01-T07) เท่านั้น น้ำหนักด้านล่างเป็นค่าที่ level-designer กำหนดเอง (ไม่ใช่ค่าเกม balance ที่ต้องอยู่ใน `config/balance/`) เพราะเป็นเครื่องมือวิเคราะห์ใช้ครั้งเดียวของ T06/T07 ไม่ใช่ค่าที่ runtime ของเกมอ่าน — ถ้า location-engineer จะใช้ซ้ำในสคริปต์ ให้ย้ายน้ำหนักเหล่านี้ไปเป็นไฟล์ `tools/coverage/analysis/launch-score.config.json` แทนการฝังในโค้ด (ตามหลัก "ไม่มี magic number ในสคริปต์" ของ METHOD หัวข้อ 1)

```
LaunchScore(ย่าน) =
    0.25 × densityScore
  + 0.25 × walkDistanceScore
  + 0.20 × transitAccessScore
  + 0.15 × presetDiversityScore
  + 0.15 × populationDensityScore
```

ทุก sub-score normalize เป็น 0–1 ด้วย min-max ข้าม 79 ย่าน (ย่านที่ดีที่สุดของตัวชี้วัดนั้นได้ 1.0 แย่ที่สุดได้ 0) เพื่อให้เทียบข้ามหน่วยกันได้ คำนวณเฉพาะย่านที่มี valid dungeon อย่างน้อย 1 แห่ง ย่านที่ไม่มีเลยได้ `LaunchScore = 0` ทันที (ไม่ normalize ให้)

## 4. นิยามแต่ละ sub-score (5 ตัวชี้วัดตามที่ board กำหนดไว้ครบ)

### 4.1 `densityScore` — จำนวน dungeon ที่ใช้ได้ต่อตร.กม.

```
rawDensity(ย่าน) = จำนวน valid dungeon ในย่าน ÷ พื้นที่ย่าน (ตร.กม.)
densityScore = minMaxNormalize(rawDensity ข้าม 79 ย่าน)
```

ใช้ต่อตร.กม.แทนจำนวนดิบ เพื่อไม่ให้ย่านที่กว้างมาก (เช่น อำเภอชานเมืองของนครปฐม) ได้เปรียบย่านที่แน่นแต่เล็กกว่าอย่างไม่เป็นธรรม — สอดคล้องกับ GDD "กลยุทธ์การเปิด" ที่ต้องการย่านที่ "dungeon หนาแน่น" ไม่ใช่ย่านที่มีจำนวนรวมมากเพราะพื้นที่ใหญ่

### 4.2 `walkDistanceScore` — ระยะเดินเฉลี่ยถึง dungeon ใกล้สุด

```
avgWalkDistance(ย่าน) = ค่าเฉลี่ยถ่วงน้ำหนักด้วยประชากรของระยะเดิน (หัวข้อ 2) จากทุกจุดตัวแทนประชากรในย่าน
                         ไปยัง valid dungeon ที่ใกล้ที่สุด (ไม่จำกัดว่าต้องอยู่ในย่านเดียวกัน — คนที่บ้านติดขอบเขตอาจใกล้ dungeon ย่านข้างๆ กว่า)
walkDistanceScore = minMaxNormalize(−avgWalkDistance ข้าม 79 ย่าน)  // ยิ่งใกล้ยิ่งได้คะแนนสูง
```

ใช้ตารางประชากรระดับกริดของ WorldPop (100 ม./ช่อง) เป็นจุดตัวแทนแทนจุดศูนย์กลางย่านเดียว เพื่อให้ค่าเฉลี่ยสะท้อนการกระจายตัวจริงของคน ไม่ใช่แค่ระยะจากศูนย์กลางเขตซึ่งอาจไม่มีคนอยู่จริง (location-engineer พิจารณาความเป็นไปได้ทางเทคนิคใน T06 ถ้าทำเต็มรูปแบบไม่ทันเวลา ให้ใช้ค่าเฉลี่ยแบบไม่ถ่วงน้ำหนักของกริดที่มีประชากร > 0 แทน และบันทึกไว้ใน `district-counts.csv`)

**ยืนยันรอบ T07:** T06 ทำได้ครบตามแบบเต็มรูปแบบจริง (ถ่วงน้ำหนักด้วยประชากรระดับกริด ไม่ใช่ทางลัด) คอลัมน์ `walk_avg_m`/`walk_median_m` และ `g1_pop_share_green` ฯลฯ ใน `district-counts.csv` มาจากระยะ network จริงทั้งหมด (`distance_method = osm_walk_network` ทุกแถว) ไม่มีแถวใดใช้ทางสำรอง 1.4 เท่า

### 4.3 `transitAccessScore` — ระยะเดินจากสถานีรถไฟฟ้า/ป้ายรถเมล์ ถ่วงน้ำหนักมากกว่าที่จอดรถ (N-6)

**หลักการ:** เกมนี้ให้รางวัลกับการเดินจริง ย่านที่เข้าถึง dungeon ได้ด้วยขนส่งสาธารณะ (เดินต่อจากลงรถไฟฟ้า/รถเมล์) ควรได้คะแนนสูงกว่าย่านที่เข้าถึงได้จริงเฉพาะคนขับรถมาจอด เพราะสอดคล้องกับกลุ่มผู้เล่นเป้าหมายและ pillar "เดินจริง" มากกว่า — **ที่จอดรถไม่ได้ให้คะแนนบวกในสูตรนี้เลย** ใช้เป็นข้อมูลประกอบเชิงคุณภาพใน coverage report เท่านั้น (ตาม N-6 "ให้น้ำหนักระยะเดินจากรถไฟฟ้า/ป้ายรถเมล์มากกว่าที่จอดรถ")

**บั๊กที่พบก่อนใช้จัดอันดับจริง (แก้ในรอบ T07, ตัดสิน — authority: level-designer):** สูตรเดิมด้านล่างทำให้ dungeon ที่มีสถานีรถไฟฟ้า "อยู่ในระยะที่ยอมรับได้" แต่ไกลกว่าป้ายรถเมล์ที่ใกล้ที่สุดมาก ได้ `transitDistance` **แย่กว่า** dungeon ที่ไม่มีรถไฟฟ้าเลย (ซึ่งใช้ `busWalkDistance` เดี่ยวๆ) เช่น รางอยู่ที่ขอบระยะยอมรับ 800 ม. แต่ป้ายรถเมล์อยู่แค่ 100 ม. → สูตรเดิมให้ (2×800+100)/3 = 567 ม. แย่กว่าไม่มีรถไฟฟ้าเลย (100 ม.) ทั้งที่มีรถไฟฟ้าควรได้แต้มเสริม ไม่ใช่แต้มลบ ขัดกับเจตนา N-6 ("ให้น้ำหนักระยะเดินจากรถไฟฟ้า/ป้ายรถเมล์มากกว่าที่จอดรถ" ไม่ใช่ "มีรถไฟฟ้าแล้วแย่กว่า")

```
railWalkDistance(dungeon) = ระยะเดิน (หัวข้อ 2) จาก dungeon ถึงสถานีรถไฟฟ้า/รถไฟที่ใกล้ที่สุด
                             (OSM: railway=station, station=subway|light_rail|train, public_transport=station)
busWalkDistance(dungeon)  = ระยะเดิน จาก dungeon ถึงป้ายรถเมล์ที่ใกล้ที่สุด (OSM: highway=bus_stop, amenity=bus_station)
railAcceptableWalk_m      = 800 ม. (ยืนยัน A-P1-F01-T06-3: ใช้ขอบโซนเขียวของ PRD หัวข้อ 3 เพราะ launch-criteria ฉบับเดิมไม่ได้ระบุตัวเลข)

สูตรเดิม (มีบั๊ก ไม่ใช้แล้ว):
  transitDistance(dungeon) = ถ้ามีรถไฟฟ้าในระยะยอมรับได้: (2 × railWalkDistance + 1 × busWalkDistance) / 3 · ไม่งั้นใช้ busWalkDistance อย่างเดียว

สูตรแก้ไข (ใช้ตั้งแต่ T07):
  transitDistance(dungeon) = ถ้ามีรถไฟฟ้าในระยะยอมรับได้:
                                min( busWalkDistance , (2 × railWalkDistance + 1 × busWalkDistance) / 3 )
                              ถ้าไม่มี: busWalkDistance อย่างเดียว
  // การมีรถไฟฟ้าจึงไม่มีวันทำให้คะแนนแย่กว่าไม่มีรถไฟฟ้าเลย (เป็นแต้มเสริมเท่านั้น ไม่ใช่แต้มลบ) และยังให้แต้มเสริมเพิ่มเมื่อรถไฟฟ้าอยู่ใกล้กว่าค่าเฉลี่ยถ่วงน้ำหนัก
transitAccessScore(ย่าน)  = minMaxNormalize(−ค่าเฉลี่ย transitDistance ของ valid dungeon ทุกแห่งในย่าน ข้ามเฉพาะย่านที่มี valid dungeon ≥ 1 แห่ง, 76 ย่าน — ยืนยัน A-P1-F01-T06-6)
```

**ผลกระทบต่ออันดับ (ตรวจก่อนใช้จัดอันดับจริง ตามที่ context ของงานนี้กำหนด):** `district-counts.csv` เก็บเฉพาะค่าเฉลี่ยระดับย่าน (`transit_mean_m`, `rail_walk_mean_m`, `bus_walk_mean_m`) ไม่ได้เก็บระยะรายชิ้น จึงคำนวณสูตรใหม่แบบแม่นยำ 100% ย้อนหลังไม่ได้จากไฟล์นี้อย่างเดียว — coverage report (P1-F01-T07) ประมาณขอบเขตบนของผลกระทบด้วย `min(transit_mean_m, bus_walk_mean_m)` ระดับย่าน (สูตรใหม่รับประกันว่าค่าจริงต่อ dungeon ดีขึ้นหรือเท่าเดิมเสมอ ไม่มีทางแย่ลง) แล้ว normalize ใหม่: **อันดับ 1–3 ไม่เปลี่ยน** (พระนคร, ปทุมวัน, ดินแดง) ผลต่างคะแนนรวมทุกย่าน ≤ 0.002 เพราะ `score_transit` เดิมของย่านอันดับต้นๆ สูงอยู่แล้ว (0.97–0.99 จาก 1.0) เหลือช่องให้ดีขึ้นน้อย — **handoff ถึง location-engineer:** รัน `tools/coverage/analysis` ซ้ำด้วยสูตรแก้ไขนี้เพื่อได้ค่าแม่นยำระดับ dungeon ก่อน F14 ใช้ตัวเลขจริง (ไม่ blocking การแนะนำย่านของ T07 เพราะอันดับ 1–3 พิสูจน์แล้วว่าไม่ขยับ)

น้ำหนัก 2:1 ระหว่างรถไฟฟ้ากับรถเมล์เป็นการเลือกของ level-designer (ไม่ใช่ตัวเลขจาก GDD) เหตุผล: รถไฟฟ้าตรงเวลาและจูงใจให้เดินทางไกลข้ามย่านได้มากกว่ารถเมล์ที่ตารางเวลาไม่แน่นอน จึงมีผลต่อ "การเดินทางมาเล่นครั้งแรก" มากกว่า — ยืนยันแล้วว่า `tools/coverage/pipeline`/`analysis` (README หัวข้อ 10.2–10.3) ดึง tag `railway=station`/`public_transport=station`/`highway=bus_stop`/`amenity=bus_station` เข้ากราฟระยะขนส่งแล้วใน T06 (handoff เดิมของฉบับก่อนปิดแล้ว)

### 4.4 `presetDiversityScore` — ความหลากหลายของ preset

```
presetDiversityScore(ย่าน) = (จำนวน preset หลักที่มีตัวแทนอย่างน้อย 1 แห่ง ใน {สวนใหญ่, ตลาด, สวนหย่อม}) ÷ 3
```

ตรงกับหน่วยของ G3/S3 ใน PRD ทุกประการ (ไม่ต้อง normalize เพิ่มเพราะเป็นสัดส่วน 0–1 อยู่แล้ว) preset ชั้นที่สอง (ริมน้ำ/ริมคลอง) ไม่นับในตัวหารนี้ ตรงกับที่ PRD หัวข้อ 5 ถ่วงน้ำหนักทางเสริมแยกต่างหากจาก dungeon หลัก

### 4.5 `populationDensityScore` — ความหนาแน่นประชากร

```
populationDensityScore(ย่าน) = minMaxNormalize(ประชากรย่าน ÷ พื้นที่ย่าน (ตร.กม.) ข้าม 79 ย่าน)
```

น้ำหนักน้อยกว่าตัวชี้วัดการเข้าถึง (4.1–4.3) โดยตั้งใจ: ความหนาแน่นสูงอย่างเดียวไม่มีประโยชน์ถ้าคนเดินไป dungeon ไม่ถึง (นั่นคือเหตุผลที่ `walkDistanceScore` มีน้ำหนักมากกว่า) แต่ยังมีน้ำหนักอยู่บ้างเพราะ GDD "กลยุทธ์การเปิด" ต้องการย่านที่ "มีคนเล่นในย่านเดียวกัน" มากพอให้ Nearby Party ทำงาน ซึ่งขึ้นกับจำนวนคนจริงในพื้นที่ ไม่ใช่แค่ระยะเดิน

### 4.6 เกณฑ์เสริม (ใหม่ รอบ T07): ทางเข้าระดับเริ่มต้นสำหรับผู้เล่นใหม่ (F-11, design gate A)

**ที่มา:** handoff จาก design gate A ข้อ F-11 — เกณฑ์ G1–G4/S1–S4 ของ PRD วัดระยะถึง valid dungeon "ที่ใกล้ที่สุด" แบบไม่แยก preset แต่คนเล่นใหม่ (level 1) ควรเจอ dungeon ที่ **เข้าได้จริงตั้งแต่ level 1** ใกล้บ้าน ไม่ใช่บังเอิญไปเจอ dungeon เลเวลสูงที่ใกล้กว่า แล้วเจอ preset ที่ไม่เหมาะกับเลเวลตัวเอง

- **นิยาม:** dungeon เริ่มต้น = preset `pocketPark` (ช่วงเลเวลแนะนำเริ่มที่ 1 ตาม `data/dungeons/presets.json#presets[].suggestedLevelRange.minLevel` ของรายการที่ `id = "pocketPark"` ค่า = 1 — ยืนยันแล้วใน P1-F03-T09 ไม่ต้องตั้งค่าใหม่ในเอกสารนี้)
- **เกณฑ์:** ย่านเปิดตัวควรมี `pocketPark` อย่างน้อย 1 แห่งอยู่ภายในระยะเดินตามโครงข่ายจริง (หัวข้อ 2) ไม่เกิน `config/balance/unlocks.json#home.farDungeonThreshold_m` (**2,000 ม.**) จากพื้นที่อยู่อาศัยหลักของย่าน — ใช้ threshold เดียวกับที่ระบบ home state ใช้ตัดสิน "ไกล" (D-064) เพื่อไม่ให้ผู้เล่นใหม่เห็นสถานะ "ไกล" ตั้งแต่วันแรกที่ระดับ 1 แม้จะยังมี dungeon อื่นที่ใกล้กว่าแต่ไม่เหมาะกับเลเวล
- **วิธีตรวจ (จำกัดข้อมูลปัจจุบัน):** `district-counts.csv` ยังไม่แยกระยะเดินตาม preset (คอลัมน์ `walk_avg_m`/`walk_median_m` นับรวมทุก preset) — ใช้ตัวแทนเบื้องต้นสองทาง: (1) จำนวน `preset_pocketPark` ในย่าน (ถ้า ≥ 1 และ `walk_median_m` โดยรวมของย่าน ≤ 2,000 ม. ให้ถือว่าผ่านแบบมีเงื่อนไข) และ (2) สัดส่วนประชากรโซนเขียว+เหลือง (`s1_pop_share_green_yellow`, เพดาน 3,000 ม.) เป็นขอบเขตบน เพราะเหลืองรวมอยู่ในช่วง ≤2,000 ม. บางส่วน — **handoff ถึง location-engineer:** เพิ่มคอลัมน์ `pocketPark_walk_median_m` (ระยะเดินถ่วงประชากรถึง `pocketPark` ที่ใกล้ที่สุดเท่านั้น) ใน `district-counts.csv` รอบถัดไป เพื่อตรวจเกณฑ์นี้ได้ตรงจริง ไม่ใช่ตัวแทน
- **ผลกับย่านที่แนะนำ:** ดู `coverage-report.md` หัวข้อการตรวจ F-11 ต่อย่าน — ใช้ตัวแทนข้างต้นประเมินว่าผ่านแบบมีเงื่อนไข ไม่ใช่ตัวเลขยืนยัน 100%
- **F14 guidance:** เมื่อวาด dungeon จริง preset `pocketPark` ต้องเริ่มช่วงเลเวลที่ 1 เสมอ (ไม่ใช่แค่ตัวเลือกแนะนำ) เพื่อให้ทุกย่านเปิดตัวมีจุดเข้าระดับเริ่มต้นจริง — อ้าง `presets.json` เดียวกัน ไม่สร้างค่าใหม่

## 5. วิธีใช้คะแนนนี้ใน T06/T07 (ไม่ทดแทนเกณฑ์ PRD)

1. P1-F01-T06 คำนวณทั้ง 5 sub-score และ `LaunchScore` ต่อ 79 ย่าน ใส่ใน `district-counts.csv` เป็นคอลัมน์เพิ่มเติม (ไม่ใช่แทนที่คอลัมน์ดิบตามหัวข้อ acceptance ของ T06)
2. P1-F01-T07 เรียง `LaunchScore` จากมากไปน้อย เลือก **5–8 ย่านบนสุด** มาตรวจกับเกณฑ์ตัวเลขจริงของ PRD F01 หัวข้อ 4 (G1–G4 / S1–S4) ทีละย่าน — `LaunchScore` สูงไม่รับประกันว่าจะผ่าน Go เพราะ PRD ใช้ค่าขั้นต่ำแบบ all-or-nothing ต่อเกณฑ์ (เช่น G2 ต้องการ ≥10 แห่งแน่นอน ไม่ใช่คะแนนเฉลี่ย) ส่วน `LaunchScore` ช่วยจัดลำดับว่าควรเริ่มตรวจย่านไหนก่อนเพื่อประหยัดเวลา ไม่ต้องไล่ตรวจทั้ง 79 ย่านด้วยมือ
3. ย่านที่คะแนนสูงแต่ไม่ผ่าน G-เกณฑ์ให้ไปดูเกณฑ์ S (Go พร้อมทางเสริม) ต่อ ตามลำดับความสำคัญของทางเสริมใน PRD หัวข้อ 5.4 (ชั้นที่สอง > ชั่วคราว > ผู้เล่นเสนอ)
4. coverage report (T07) ต้องแสดงตาราง 5 sub-score + `LaunchScore` ของย่านที่แนะนำทุกย่าน เพื่อให้ HUMAN (P1-F01-T11) เห็นเหตุผลเชิงตัวเลขของการจัดลำดับ ไม่ใช่แค่ผลผ่าน/ไม่ผ่าน

## 6. `verification_mode` และ `floor_level` (v1)

ทุก candidate ที่ใช้คำนวณคะแนนในเอกสารนี้ต้องมี `verification_mode = "continuous_gps"` และ `floor_level = null` ตามที่ `config/balance/dungeons.json#verification` กำหนด และตามที่ `design/levels/dungeon-rules.md` หัวข้อ 13 อธิบายไว้แล้ว — เอกสารนี้ไม่ให้คะแนนหรือถ่วงน้ำหนักพิเศษกับ candidate ในอาคาร เพราะ v1 ไม่มี candidate ประเภทนั้นอยู่แล้ว (non-negotiable ข้อ 5 ของ CLAUDE.md)

## 7. ข้อจำกัดของคะแนนนี้

- ต้องมี candidate ผ่าน T05 ครบก่อนจึงคำนวณได้จริง (เอกสารนี้เขียนก่อนเห็นข้อมูล ตามลำดับเวลาเดียวกับ PRD F01 ที่เขียนก่อนเห็น `candidates.geojson`) — **อัปเดตรอบ T07:** ข้อจำกัดนี้หมดแล้ว T06 รันจริงครบ 730 dungeon แล้ว
- ~~`walkDistanceScore` และ `transitAccessScore` ต้องพึ่ง route factor ประมาณการ~~ **แก้แล้วรอบ T07:** T06 ใช้ network routing จริงตาม PRD 8.1 ไม่ได้ใช้ route factor ประมาณการเลย (ดูหัวข้อ 2) เหลือเพียง `transitAccessScore` ที่ยังมีบั๊กสูตรซึ่งแก้ในหัวข้อ 4.3 แล้ว (ผลต่ออันดับ 1–3 พิสูจน์แล้วว่าไม่เปลี่ยน)
- น้ำหนัก 0.25/0.25/0.20/0.15/0.15 และน้ำหนักย่อย 2:1 ของ transit เป็นดุลยพินิจของ level-designer ไม่ใช่ตัวเลขจาก GDD — ถ้า design gate (P1-F01-T09) เห็นว่าน้ำหนักผิดสัดส่วน ให้แก้ในเอกสารนี้ได้ก่อนที่ T07 จะใช้จัดอันดับจริง (หลักการเดียวกับ PRD หัวข้อ 8: ห้ามแก้เกณฑ์หลังเห็นผลจริงแล้ว)
- เกณฑ์ 4.6 (F-11 ทางเข้าระดับเริ่มต้น) ยังตรวจด้วยตัวแทน ไม่ใช่ระยะเดินเฉพาะ `pocketPark` จริง — รอคอลัมน์ใหม่จาก location-engineer ตามที่ระบุในหัวข้อนั้น

