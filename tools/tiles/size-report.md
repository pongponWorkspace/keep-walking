# Size report — tile กรุงเทพฯ + ปริมณฑล (P1-F02-T06)

วันที่วัด 2026-09-23 · เครื่อง macOS arm64 (10 core) · ผลดิบซ้ำได้ด้วย `tools/tiles/bin/build.sh` (เขียน `tools/tiles/out/size-report.{json,txt}`) · ค่าเพดานตาม tech note 7.1 และ 7.4 อยู่ใน `config.json#budget`

## 1. ผลหลัก: `pm4-20260923-z15` ผ่านงบ Cloudflare Pages Free

| รายการ | ค่าจริง | เป้า / เพดาน | ผล |
| --- | --- | --- | --- |
| tile XYZ | 13,502 ไฟล์ | เป้า ≤ 15,000 (90.0%) | ผ่าน |
| ไฟล์ทั้ง deploy | 14,281 + 2 สำรองให้ `_headers`, `404.html` = 14,283 | เป้า ≤ 16,000 (89.3%) · เพดาน 20,000 (71.4%) | ผ่าน |
| ไฟล์ใหญ่สุด | 300,539 B (`15/25531/15119.mvt`) | เพดาน 25 MiB ต่อไฟล์ | ผ่าน (ห่าง 87 เท่า) |
| byte ของ tile (MVT ไม่บีบอัด) | 109,132,780 B (104.08 MiB) | ไม่มีเพดาน | รายงานไว้ |
| byte ทั้ง deploy | 120,328,128 B (114.75 MiB) | ไม่มีเพดาน | รายงานไว้ |
| PMTiles (gzip ต่อ tile) | 71,592,019 B (68.28 MiB) | ไม่ publish ขึ้น Pages | ใช้ local, GitHub Pages สำรอง, R2 |
| เวลา build | 113 วินาที ครั้งแรก (extract 33 วินาที + แตก XYZ ~60 วินาที + ที่เหลือ) · 79 วินาที เมื่อมี archive แล้ว | — | — |

ชุดไฟล์: tile 13,502 · glyph PBF 768 (3 fontstack × 256) · font-faces 2 · sprite 4 + license 1 · license font 2 · `tiles.json` 1 · `manifest.json` 1

แหล่ง: Protomaps Basemap v4 build `20260923` (metadata 4.15.2, OSM replication 2026-09-23T04:00:00Z) · bbox `99.80,13.40,101.00,14.35` · z0–15 · client overzoom ถึง z18

## 2. ตารางต่อ zoom

| zoom | ไฟล์ | byte (ไม่บีบอัด) | tile ใหญ่สุด (B) |
| --- | --- | --- | --- |
| 0–6 | 7 (zoom ละ 1) | 621,450 | 143,921 (z3) |
| 7 | 2 | 164,367 | 92,538 |
| 8 | 4 | 245,150 | 74,948 |
| 9 | 6 | 428,920 | 106,804 |
| 10 | 20 | 1,173,764 | 191,560 |
| 11 | 56 | 2,075,457 | 176,676 |
| 12 | 195 | 7,511,723 | 231,527 |
| 13 | 672 | 14,102,794 | 208,357 |
| 14 | 2,530 | 25,150,607 | 179,525 |
| 15 | 10,010 | 57,658,548 | 300,539 |
| รวม | 13,502 | 109,132,780 | 300,539 |

z15 คือ 74% ของไฟล์และ 53% ของ byte · จำนวน 13,502 ตรงกับ `pmtiles extract --dry-run` ของ tech note 5.2 (tile entry หลัง dedup 12,713 แต่ XYZ ต้องมีไฟล์ต่อพิกัด จึงนับ 13,502)

## 3. การเลือก zoom และผลต่อการอ่านชื่อถนน

- Protomaps v4 มี maxzoom 15 โดยออกแบบ: ข้อมูลรายละเอียดเต็มอยู่ที่ z15 · z16–18 เป็น overzoom เสมอไม่ว่า host ไหน (รวม R2) · vector tile ขยายได้โดยเส้นไม่แตก ป้ายถูกวางใหม่ที่ทุก zoom · ดังนั้น **z15 ไม่เสียรายละเอียดใดเทียบกับชุดเต็มของ Protomaps**
- ตรวจ tile ลุมพินี z15 `25535/15122`: มี `ถนนพระรามที่ 4`, `ถนนราชดำริ`, `ถนนวิทยุ` เป็นภาษาไทยใน `name` · ไม่มี key `name:th` ใน build นี้ (แจ้ง art-director ในหัวข้อ 8)
- ถ้าต้องใช้ทางแก้ z14 (tile 3,492 ไฟล์): เทียบ tile z14 `12767/7561` กับลูก 4 tile ที่ z15 รอบลุมพินี

| ชั้น | z14 (1 tile) | z15 (4 tile พื้นที่เดียวกัน) | ผลต่อผู้เล่น |
| --- | --- | --- | --- |
| ถนน major / minor / path | 44 / 228 / 128 | 60 / 350 / 199 | ถนนทุกประเภทยังอยู่ (เส้นถูกลดจุด) |
| ถนนที่มีชื่อ (ชื่อไม่ซ้ำ) | 247 | 271 | ชื่อถนน 91% ยังอ่านได้เมื่อ overzoom |
| POI | 284 | 7,060 | POI ย่อยหาย เหลือเฉพาะที่สำคัญ |
| อาคาร | 178 | 10,311 | รูปอาคารแทบหายทั้งหมด |

  → z14 ยังเดินตามถนนได้ แต่ภาพเมืองโล่งลงชัดเจน (ไม่มีอาคาร) · ใช้เป็นทางแก้เท่านั้น

## 4. ทางเลือกที่วัดแล้วและการทดสอบลำดับทางแก้

| ทางเลือก | tile | PMTiles | XYZ (ไม่บีบอัด) | หมายเหตุ |
| --- | --- | --- | --- | --- |
| **bbox z15 (เลือก)** | 13,502 | 68.28 MiB | 104.08 MiB | ผ่านเป้า เหลือ 1,717 ไฟล์ก่อนเป้า 16,000 |
| bbox z14 (ทางแก้ 2) | 3,492 | 35.37 MiB | 49.09 MiB | ไฟล์รวม 4,273 · extract จาก z15 ในเครื่อง ไม่ใช้เน็ต |
| polygon 6 จังหวัด z15 (`--region-mode provinces`) | 7,789 | 58 MB | ไม่ได้ build | เหลือที่ขยายพื้นที่มากกว่า แต่นอกเขตจังหวัดเป็นช่องว่าง · โซนดำ (D-043) บังส่วนนั้นอยู่แล้ว จึงเป็นคันโยกสำรองถ้าต้องเพิ่มจังหวัด |

ทดสอบลำดับทางแก้ด้วย `TILES_CONFIG` ที่ตั้งงบให้แคบลง (ไม่แก้ config จริง):

| กรณี | ผลที่คาด | ผลจริง |
| --- | --- | --- |
| งบจริง | exit 0 ที่ z15 | exit 0 · `RESULT PASS Cloudflare Pages Free budget` |
| `targetMaxTileFiles = 5000` | ลดเป็น z14 แล้วผ่าน | `fallback step 2: maxzoom 15 -> 14` · tile 3,492 · exit 0 |
| `targetMaxTileFiles = 1000` | z14 ยังเกิน → ชุด GitHub Pages | `fallback step 3` · `out/publish-ghpages/` = PMTiles z15 71.6 MB + 778 ไฟล์ · exit 3 |
| เพิ่ม `githubPagesMaxSiteBytes = 1000`, `--no-fallback` | หยุดถาม HUMAN | `fallback step 4: no free host limit fits` · exit 4 |

ทุกกรณีที่ไม่ผ่าน ชุดที่เกินงบถูกย้ายไป `out/publish-over-budget/` เพื่อไม่ให้ถูก deploy โดยไม่ตั้งใจ

## 5. ประมาณการทั้งประเทศไทย เทียบ GDD และ R2

GDD หัวข้อสถาปัตยกรรม (แผนที่): "Extract เฉพาะประเทศไทยจะได้ไฟล์ราว 1–3 GB" · วัดด้วย `bin/estimate-area.sh` (dry-run กับ build ที่ pin ไม่ดาวน์โหลด tile)

| พื้นที่ | maxzoom | tile | PMTiles |
| --- | --- | --- | --- |
| เส้นขอบประเทศ (union 77 จังหวัดจาก geoBoundaries ADM1 ใน `tools/coverage/downloads/`) | 15 | 506,975 | 592 MB |
| เส้นขอบประเทศ | 14 | 128,998 | 315 MB |
| bbox ประเทศ `97.34,5.61,105.64,20.47` (รวมทะเลและประเทศเพื่อนบ้าน) | 15 | 1,406,403 | 863 MB |
| bbox ประเทศ | 14 | 352,539 | 451 MB |

- ขนาดจริงทั้งประเทศที่ z15 ≈ **0.6 GB** (ตามเส้นขอบ) ถึง 0.86 GB (bbox) · ต่ำกว่าตัวเลข 1–3 GB ของ GDD · ต้นทุน storage บน R2 จึงต่ำกว่าที่ GDD ประมาณ (ตัวเลขราคาไม่อยู่ในงานนี้ ต้องตรวจราคาปัจจุบันก่อนตัดสิน ตาม GDD)
- **XYZ บน Pages Free ทั้งประเทศทำไม่ได้:** 506,975 tile = 25 เท่าของเพดาน 20,000 ไฟล์ · แม้ z14 ก็ 6.4 เท่า · ขยายเกิน 6 จังหวัดจึงต้องใช้ PMTiles บน host ที่ตอบ `206` (R2 ตาม GDD, D-001 ต้องให้คนอนุมัติการผูกช่องทางชำระเงิน) หรือแบ่ง Pages หลาย project (ไม่แนะนำ: ต้องมี logic เลือก project ฝั่ง client)
- ด้วยงบปัจจุบัน Pages Free รองรับพื้นที่ประมาณ 1.1 เท่าของ bbox นี้ที่ z15 (ตามอัตรา tile ต่อพื้นที่แบบเมือง) หรือราว 1.9 เท่าถ้าใช้ `--region-mode provinces`

### 5.1 เทียบรูปแบบ R2 (อนาคต, tech note 7.6)

| รายการ | Pages Free XYZ (ตอนนี้) | R2 PMTiles (อนาคต) |
| --- | --- | --- |
| ไฟล์ tile ของ 6 จังหวัด | 13,502 ไฟล์ · 104 MiB (Cloudflare บีบอัดตอนส่ง) | 1 ไฟล์ · 68.3 MiB (gzip ต่อ tile ในไฟล์) |
| ทั้งประเทศ z15 | ทำไม่ได้ (เกินเพดานไฟล์) | 1 ไฟล์ · ~592 MB (ไม่มีเพดานต่อไฟล์แบบ Pages) |
| glyph + font-faces + sprite | 777 ไฟล์ · ~11 MB | ชุดเดียวกัน |
| request ต่อ tile | 1 GET ต่อ tile (cache ได้ที่ edge ด้วย URL คงที่) | 1 range GET ต่อ tile + directory เป็นครั้งคราว |
| เปลี่ยนฝั่ง client | — | เปลี่ยน `VITE_TILES_URL` เป็น `pmtiles://...` เท่านั้น (tech note 8) |
| เงื่อนไข | ฟรี ไม่ผูกบัตร (D-008) | ต้องผูกช่องทางชำระเงิน → HUMAN อนุมัติ (D-001) |

## 6. การตรวจ bbox กับขอบเขต 6 จังหวัด (ปิด A-P1-F02-T03-2)

`python3 tools/tiles/bin/verify-bbox.py` กับ `tools/coverage/out/boundaries.geojson` (OSM, data date 2026-09-01, 79 อำเภอ)

| จังหวัด | min_lon | min_lat | max_lon | max_lat | อยู่ใน bbox |
| --- | --- | --- | --- | --- | --- |
| TH-10 กรุงเทพมหานคร | 100.3279 | 13.4907 | 100.9386 | 13.9552 | ใช่ |
| TH-12 นนทบุรี | 100.2633 | 13.7891 | 100.5676 | 14.1407 | ใช่ |
| TH-13 ปทุมธานี | 100.3316 | 13.9163 | 100.9521 | 14.2760 | ใช่ |
| TH-11 สมุทรปราการ | 100.4444 | 13.4776 | 100.9639 | 13.7185 | ใช่ |
| TH-74 สมุทรสาคร | 100.0269 | 13.4252 | 100.4201 | 13.7246 | ใช่ |
| TH-73 นครปฐม | 99.8167 | 13.6486 | 100.3376 | 14.1791 | ใช่ |
| union | 99.8167 | 13.4252 | 100.9639 | 14.2760 | ใช่ |

ระยะเผื่อจากขอบจังหวัดถึงขอบ bbox: ตะวันตก 1,806 ม. · ตะวันออก 3,902 ม. · ใต้ 2,788 ม. · เหนือ 8,178 ม. · ทุกด้านมากกว่า 1 tile z15 (~1.2 กม.) → **bbox `99.80,13.40,101.00,14.35` ครอบครบ ไม่ต้องแก้** · ด้านตะวันตก (นครปฐม) เผื่อน้อยที่สุด ถ้าเพิ่มจังหวัดทางตะวันตกต้องขยาย bbox

## 7. ขนาดที่ดาวน์โหลดและดิสก์

| ของ | ขนาด |
| --- | --- |
| go-pmtiles 1.31.2 (macOS arm64 zip) | 15,963,714 B |
| basemaps-assets tarball @028c18f | 6,379,327 B |
| Noto Sans Thai v2.002 zip | 4,720,990 B |
| `pmtiles extract` z15 (ข้อมูลที่ส่งจริง) | 75 MB |
| ดิสก์หลัง build (`downloads/` + `out/`) | ~93 MB + ~250 MB (รวม z14 จากการทดสอบทางแก้) |

## 8. ข้อสังเกตสำหรับงานอื่น

- **art-director (P1-F03-T12):** build `20260923` ไม่มี property `name:th` · ชื่อไทยอยู่ใน `name` และมี `name:en` · style ที่ตั้ง `lang: "th"` ต้อง fallback ไป `name` (ตามที่ tech note 6.2 คาดไว้) · ตรวจกับ fixture ได้ทันที
- **gameplay-programmer (P1-F02-T11):** `tiles.json` ใช้ URL เต็ม เพราะ MapLibre 6.10 (`src/source/load_tilejson.ts`) ไม่ resolve `tiles[]` กับ URL ของ TileJSON · ใช้ `set-public-url.sh` เมื่อย้าย fixture ไปเสิร์ฟจาก dev server
- **devops-engineer (P1-F02-T08):** deploy โฟลเดอร์ `out/publish/` ทั้งโฟลเดอร์ · PMTiles อยู่นอกโฟลเดอร์นี้ (`out/pmtiles/`) · งบกันที่ไว้ 2 ไฟล์สำหรับ `_headers` และ `404.html`
