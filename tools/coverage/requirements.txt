# tools/coverage Python dependencies (ADR 0001 section 3.11).
# Exact pins, including transitive dependencies (pip freeze of a clean venv).
# Install into tools/coverage/.venv only, never globally. Python 3.11 or newer.
# Tested on macOS arm64, Python 3.14.6, 2026-09-23.

# direct
osmium==4.3.1
shapely==2.1.2
pyproj==3.8.0
numpy==2.5.3
pytest==9.1.1

# transitive
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.20
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
requests==2.34.2
urllib3==2.8.0
