# Prettier checks code and code config only. Docs, design data, and config
# values are owned by other roles and are not reformatted by CI.
pnpm-lock.yaml
node_modules/
dist/
.wrangler/
/reports/
test-results/
playwright-report/
*.md
*.pmtiles
*.geojson
config/
data/
design/
studio/
product/
art/
audio/
ops/
qa/playtest/
.claude/

# Tool outputs, downloads and virtualenvs (P1-X05). Also in .gitignore, listed here so
# prettier stays correct even when run with --ignore-path .prettierignore only.
tools/coverage/.venv/
tools/coverage/out/
tools/coverage/downloads/
tools/tiles/out/
tools/tiles/downloads/

# Generated files that must stay byte-exact (sizes and sha256 are recorded in manifests;
# `pnpm format` must never rewrite them). Regenerate with the owning tool instead.
tools/tiles/fixtures/
